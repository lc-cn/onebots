{
  "name": "@onebots/adapter-line",
  "version": "1.0.7",
  "description": "onebots Line Messaging API 适配器（轻量版，支持 Node.js 和 Cloudflare Workers）",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "rm -f *.tsbuildinfo && tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "clean": "rm -rf lib *.tsbuildinfo"
  },
  "keywords": [
    "onebots",
    "line",
    "adapter",
    "messaging-api",
    "cloudflare-workers",
    "serverless"
  ],
  "author": "凉菜",
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts"
  ],
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsc-alias": "latest",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "onebots": "workspace:*"
  },
  "peerDependenciesMeta": {
    "https-proxy-agent": {
      "optional": true
    }
  },
  "optionalDependencies": {
    "https-proxy-agent": "^7.0.6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lc-cn/onebots.git"
  }
}
