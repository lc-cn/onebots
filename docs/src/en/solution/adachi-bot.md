# Adachi-BOT

Adachi-BOT is registered in the unified Application catalog with the `planned` stage. It cannot be activated with `-t adachi-bot` yet; OneBots rejects startup instead of reporting research notes as working support.

| Item | Current value |
| --- | --- |
| Application ID | `adachi-bot` |
| Candidate protocols | `onebot.v11` |
| Stage | `planned` |

## Missing implementation

The distribution's actual private-action usage still needs a source audit.

Promotion to `available` requires a same-name Application package or built-in implementation, protocol capability descriptors, and a pinned-version interoperability gate.
