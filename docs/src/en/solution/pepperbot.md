# PepperBot

PepperBot is registered in the unified Application catalog with the `planned` stage. It cannot be activated with `-t pepperbot` yet; OneBots rejects startup instead of reporting research notes as working support.

| Item | Current value |
| --- | --- |
| Application ID | `pepperbot` |
| Candidate protocols | `onebot.v11` |
| Stage | `planned` |

## Missing implementation

Maintenance status and compatibility with current Python versions need confirmation.

Promotion to `available` requires a same-name Application package or built-in implementation, protocol capability descriptors, and a pinned-version interoperability gate.
