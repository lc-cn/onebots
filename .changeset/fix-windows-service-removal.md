---
"onebots": patch
---

修复 Windows 服务安装与卸载时重复拼接盘符或 UNC 根路径的问题；读取 DACL 保护的服务元数据和恢复日志时不再误用 NTFS 的 POSIX mode 映射，确保首次安装后可以启动及卸载 SCM 服务；卸载时接受 NTFS 已删除目录项但仍由安全句柄锚定的删除待决状态，删除服务定义后改用固定 SCM 身份确认服务不存在；原生生命周期按管理端 `/ready` 契约校验实例身份，并等待严格状态快照收敛。
