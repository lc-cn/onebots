---
"onebots": patch
---

增加 Windows SCM 管理服务底层驱动与 x64/arm64 原生宿主工件，并建立仅允许 manager 进程发布、控制 SID 使用最小权限的本地命名管道协议。Windows CLI 通过随包原生桥的标准输入安全转发管理请求，不因 Node 通用管道权限被拒绝，也不放宽管道 DACL。生命周期写操作在完整恢复控制面可验收前继续失败关闭，不会留下半安装状态。
