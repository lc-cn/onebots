---
"onebots": patch
---

将系统服务的首次安装和启停命令接入独立管理服务契约。安装不再要求平台或协议配置，服务操作保留网关期望状态，并在操作结果未知时保留记录、禁止盲目重试。统一安装、迁移及控制校验的服务定义，避免 macOS 日志路径不一致导致新装服务无法控制。

状态命令分别报告管理服务与网关状态，并删除已由统一控制客户端替代、没有生产入口的旧 TUI 链路。

处理 macOS 验证子进程组退出时短暂返回权限未知的情况：在固定期限内继续核实，仅在明确不存在后释放所有权，超时仍保留恢复记录。
